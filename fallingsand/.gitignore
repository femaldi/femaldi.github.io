# =============================================================================
# A good gitignore for an Electron + Node.js project.
# =============================================================================

# Dependencies
# -----------------------------------------------------------------------------
# This folder is massive and can be regenerated by running `npm install`.
# Never commit this.
/node_modules

# Build output
# -----------------------------------------------------------------------------
# This is the folder where electron-builder places the final executables and
# installers. It's generated from your source code.
/dist

# Electron-builder also generates a directory for unpacked files during build.
/release-builds

# The ASAR archive file containing your app's source code.
*.asar

# Logs
# -----------------------------------------------------------------------------
# Log files created by npm, yarn, or your application.
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment Variables
# -----------------------------------------------------------------------------
# Files containing sensitive information like API keys or local settings.
# A .env.example file should be committed instead as a template.
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Operating System Files
# -----------------------------------------------------------------------------
# Metadata files created by macOS and Windows.
.DS_Store
Thumbs.db

# IDE / Editor Configuration
# -----------------------------------------------------------------------------
# User-specific settings for code editors like VS Code, JetBrains IDEs, etc.
.idea
.vscode
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw? # Vim swap files